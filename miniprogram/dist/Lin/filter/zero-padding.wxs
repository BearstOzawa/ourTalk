function zeroPadding(num, length) {
    length = undefined === length ? 2 : length;
    if ((num + "").length >= length) {
        return (num)
    };
    return (zeroPadding("0" + num, length))
};
module.exports = zeroPadding;